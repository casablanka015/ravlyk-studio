---
const { title, description, lessons, videoPreview } = Astro.props;
---

<div class="border rounded-lg p-6 shadow hover:shadow-lg transition duration-300 bg-white">
  <video
    class="w-full h-40 rounded mb-4 object-cover"
    src={videoPreview}
    controls
    preload="metadata"
  ></video>
  <h3 class="text-xl font-semibold mb-2">{title}</h3>
  <p class="text-gray-600 mb-3">{description}</p>
  <p class="text-sm text-gray-500 mb-4">Кількість уроків: {lessons}</p>
  <button class="bg-black text-white px-4 py-2 rounded hover:bg-gray-800 transition">
    Дивитись
  </button>
</div>
